[
        [Memory.getStateParam("interactionState") == "active" -->
        TTS.speak("Hey, I am Temi"); Memory.setStateParam("interactionState", "changing to idle"); TiltHead.up()]

        [Memory.getStateParam("interactionState") == "changing to idle" -->
        TTS.speak("I'm going to sleep now"); Memory.setStateParam("interactionState", "idle");
        TiltHead.down()]
    ]
