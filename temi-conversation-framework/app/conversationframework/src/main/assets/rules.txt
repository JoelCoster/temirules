[   
    [Memory.getStateParam("interactionState") == "Active" && Memory.getStateParam("askedAboutDay") == "True" -->
    TiltHead.up();
    TTS.speak("How can I help you?");
    ASR.listen();
    Memory.setStateParam("interactionState", "waitingForInput")]

    [Memory.getStateParam("interactionState") == "Active" -->
    TiltHead.up();
    TTS.speak("Hey there, I am Temi. How is your day?");
    ASR.listen();
    Memory.setStateParam("interactionState", "waitingForInput")]

    [Memory.getStateParam("interactionState") == "asrReceived" && Memory.getStateParam("lastAsrResult") == "go home" -->
    TTS.speak("Going home to my charging station");
    Move.goHome();
    Memory.setStateParam("interactionState", "processing")]

    [Memory.getStateParam("interactionState") == "asrReceived" && Memory.getStateParam("lastAsrResult") == "test pattern" -->
    TTS.speak("Pattern test worked");
    Memory.setStateParam("interactionState", "processing")]

    [Memory.getStateParam("interactionState") == "asrReceived" && PatternMatch("go to {location}", Memory.getStateParam("lastAsrResult")) -->
    TTS.speak("Going to");
    TTS.speak(ExtractParam("go to {location}", Memory.getStateParam("lastAsrResult")));
    Move.goToLocation(ExtractParam("go to {location}", Memory.getStateParam("lastAsrResult")));
    Memory.setStateParam("interactionState", "processing")]

    [Memory.getStateParam("interactionState") == "asrReceived" && PatternMatch("go to {location}", Memory.getStateParam("lastAsrResult")) -->
    TTS.speak("Going to");
    TTS.speak(ExtractParam("go to {location}", Memory.getStateParam("lastAsrResult")));
    Move.goToLocation(ExtractParam("go to {location}", Memory.getStateParam("lastAsrResult")));
    Memory.setStateParam("interactionState", "processing")]

    [Memory.getStateParam("interactionState") == "asrReceived" && PatternMatch("take me to {location}", Memory.getStateParam("lastAsrResult")) -->
    TTS.speak("Taking you to");
    TTS.speak(ExtractParam("take me to {location}", Memory.getStateParam("lastAsrResult")));
    Move.goToLocation(ExtractParam("take me to {location}", Memory.getStateParam("lastAsrResult")));
    Memory.setStateParam("interactionState", "processing")]

    [Memory.getStateParam("interactionState") == "asrReceived" && PatternMatch("let's go to {location}", Memory.getStateParam("lastAsrResult")) -->
    TTS.speak("Let's go to");
    TTS.speak(ExtractParam("let's go to {location}", Memory.getStateParam("lastAsrResult")));
    Move.goToLocation(ExtractParam("let's go to {location}", Memory.getStateParam("lastAsrResult")));
    Memory.setStateParam("interactionState", "processing")]

    [Memory.getStateParam("interactionState") == "asrReceived" && PatternMatch("open {app}", Memory.getStateParam("lastAsrResult")) -->
    TTS.speak("Opening");
    TTS.speak(ExtractParam("open {app}", Memory.getStateParam("lastAsrResult")));
    System.openApp(ExtractParam("open {app}", Memory.getStateParam("lastAsrResult")));
    Memory.setStateParam("interactionState", "processing")]

    [Memory.getStateParam("interactionState") == "asrReceived" && Memory.getStateParam("lastAsrResult") == "follow me" -->
    TTS.speak("I will follow you now");
    Move.follow();
    Memory.setStateParam("interactionState", "processing")]

    [Memory.getStateParam("interactionState") == "asrReceived" && Memory.getStateParam("lastAsrResult") == "stop following" -->
    TTS.speak("I have stopped following");
    Move.stopFollowing();
    Memory.setStateParam("interactionState", "processing")]

    [Memory.getStateParam("interactionState") == "asrReceived" && Memory.getStateParam("lastAsrResult") == "which locations do you know" -->
    TTS.speak("Here are my saved locations:");
    TTS.speak(Locations.listLocations());
    Memory.setStateParam("interactionState", "processing")]

    [Memory.getStateParam("interactionState") == "asrReceived" && Memory.getStateParam("lastAsrResult") == "save this location" -->
    TTS.speak("What would you like to call this location?");
    ASR.listen();
    Memory.setStateParam("interactionState", "waitingForLocationName")]

    [Memory.getStateParam("interactionState") == "asrReceived" && Memory.getStateParam("lastAsrResult") == "can you go somewhere for me" -->
    TTS.speak("Where should I go?");
    ASR.listen();
    Memory.setStateParam("interactionState", "waitingForLocationToGo")]

    [Memory.getStateParam("interactionState") == "asrReceived" && Memory.getPreviousStateParam("interactionState") == "waitingForLocationToGo" -->
    TTS.speak("Going to");
    TTS.speak(Memory.getStateParam("lastAsrResult"));
    Move.goToLocation(Memory.getStateParam("lastAsrResult"));
    Memory.setStateParam("interactionState", "processing")]

    [Memory.getStateParam("interactionState") == "asrReceived" && Memory.getPreviousStateParam("interactionState") == "waitingForLocationName" -->
    TTS.speak("Saving current location as");
    TTS.speak(Memory.getStateParam("lastAsrResult"));
    Memory.setStateParam("locationSaved", Locations.saveCurrentLocation(Memory.getStateParam("lastAsrResult")));
    Memory.setStateParam("interactionState", "processing")]

    [Memory.getStateParam("interactionState") == "asrReceived" && Memory.getStateParam("lastAsrResult") == "how many locations do you know" -->
    TTS.speak("I have saved");
    TTS.speak(Locations.getLocationCount());
    TTS.speak("locations");
    Memory.setStateParam("interactionState", "processing")]

    [Memory.getStateParam("interactionState") == "asrReceived" && PatternMatch("can you ask gpt {prompt}", Memory.getStateParam("lastAsrResult")) -->
    Memory.setStateParam("gptPrompt", ExtractParam("can you ask gpt {prompt}", Memory.getStateParam("lastAsrResult")));
    TTS.speak("Let me ask GPT for you.");
    Memory.setStateParam("gptResponse", HuggingFace.ask(Memory.getStateParam("gptPrompt")));
    TTS.speak(Memory.getStateParam("gptResponse"));
    Memory.setStateParam("interactionState", "processing")]

    [Memory.getStateParam("interactionState") == "asrReceived" && Memory.getStateParam("askedAboutDay") == "True" -->
    Memory.setStateParam("gptResponse", HuggingFace.ask(Memory.getStateParam("lastAsrResult")));
    TTS.speak(Memory.getStateParam("gptResponse"));
    Memory.setStateParam("interactionState", "processing")]

    [Memory.getStateParam("interactionState") == "asrReceived" -->
    TTS.speak("You said the following:");
    TTS.speak(Memory.getStateParam("lastAsrResult"));
    TTS.speak("I'll go back to sleep now. Say hey Temi to wake me up again.");
    Memory.setStateParam("askedAboutDay", "True");
    Memory.setStateParam("interactionState", "processing")]

    [Memory.getStateParam("interactionState") == "processing" -->
    TiltHead.down();
    Memory.setStateParam("interactionState", "sleeping")]

]